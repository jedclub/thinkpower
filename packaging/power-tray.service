[Unit]
Description=ThinkPower 4-Stage Power & Battery Indicator
After=graphical-session.target
PartOf=graphical-session.target

[Service]
ExecStart=/usr/bin/power-tray
ExecStop=/usr/bin/power-profile-manager restore
Restart=always
RestartSec=2

[Install]
WantedBy=graphical-session.target
